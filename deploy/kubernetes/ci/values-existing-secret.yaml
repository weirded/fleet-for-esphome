server:
  url: http://homeassistant.local:8765
  existingSecret: fleet-token-from-elsewhere
  existingSecretTokenKey: SERVER_TOKEN

autoscaling:
  enabled: true
  minReplicaCount: 1
  maxReplicaCount: 3
