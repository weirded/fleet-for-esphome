server:
  url: http://homeassistant.local:8765
  token: ci-test-token-do-not-deploy

autoscaling:
  enabled: true
  minReplicaCount: 0
  maxReplicaCount: 5
  cooldownPeriod: 300
  pollingInterval: 30
  targetQueueSize: 2

worker:
  tags:
    - k8s
    - bursty
