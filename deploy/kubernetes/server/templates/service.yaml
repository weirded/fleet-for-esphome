apiVersion: v1
kind: Service
metadata:
  name: {{ include "esphome-fleet-server.fullname" . }}
  labels:
    {{- include "esphome-fleet-server.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "esphome-fleet-server.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
