aiohttp>=3.14.3
pydantic>=2.13.4
croniter>=6.2.4
apscheduler>=3.11.3,<4
# Upper bound is deliberate. 46.0.0 landed ConnectionClosedEvent for API
# consumers (esphome/aioesphomeapi#1853); our test suite stubs the whole
# library out (tests/test_device_poller.py does
# sys.modules.setdefault("aioesphomeapi", MagicMock())), so CI cannot
# detect a 46.x behaviour change on the live device-polling path. Floor
# bumped to the version dependabot asked for; the 46.x move gets its own
# evaluation. See DEP.3 in dev-plans/WORKITEMS-1.8.md.
aioesphomeapi>=46.3.0,<47
zeroconf>=0.150.0
pyyaml>=6.0.3
# SE.1: esphome was dropped from the server image in 1.4.1. At first
# boot, scanner.ensure_esphome_installed lazy-installs the HA ESPHome
# add-on's version into /data/esphome-versions/<ver>/ via the shared
# VersionManager. This eliminates #51 (bundle upgrade friction) + #56
# (macOS-only transitives leaking in) + #20 (upstream API changes
# invisible until re-bump) in one go. The image is ~200 MB smaller.
requests>=2.34.2
icmplib>=3.0.4
# #108 briefly added py-spy here for external `docker exec` thread
# dumps. #189 replaced the py-spy path with an in-process frame walk
# (``diagnostics.in_process_thread_dump``) that works inside every
# HA install variant without privileged capabilities, so py-spy was
# removed to save ~5 MB/image. ``scripts/threaddump-addon.sh`` still
# ``pip install py-spy`` inside its own throwaway sidecar for the
# deadlocked-interpreter edge case, so that path is unaffected.

# Transitive of requests/aiohttp; floor bumped to clear PYSEC-2026-215 (fix in 3.15).
idna>=3.19

# Transitive of aioesphomeapi / noiseprotocol / chacha20poly1305-reuseable
# (the Noise-encrypted ESPHome API path). Floor bumped to clear
# PYSEC-2026-3552 / CVE-2026-69247 — a Bleichenbacher oracle in PKCS#7
# EnvelopedData decryption (pkcs7_decrypt_der/_pem/_smime), introduced in
# 44.0.0 and fixed in 50.0.0. Fleet never calls those APIs; the only reason
# cryptography is in the image is the Noise handshake, so this was never
# exploitable here. Bumped rather than PY-7-ignored because 50.0.0 resolves
# cleanly against our pins — an ignore would have to be re-justified on
# every future audit run, and a real fix does not.
cryptography>=50
