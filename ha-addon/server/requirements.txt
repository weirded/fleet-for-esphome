aiohttp>=3.9
pydantic>=2.13.3
croniter>=6.2.2
apscheduler>=3.11.2,<4
aioesphomeapi>=44.22.0
zeroconf>=0.131
pyyaml>=6.0.3
# SE.1: esphome was dropped from the server image in 1.4.1. At first
# boot, scanner.ensure_esphome_installed lazy-installs the HA ESPHome
# add-on's version into /data/esphome-versions/<ver>/ via the shared
# VersionManager. This eliminates #51 (bundle upgrade friction) + #56
# (macOS-only transitives leaking in) + #20 (upstream API changes
# invisible until re-bump) in one go. The image is ~200 MB smaller.
requests>=2.31
icmplib>=3.0.4
# #108 briefly added py-spy here for external `docker exec` thread
# dumps. #189 replaced the py-spy path with an in-process frame walk
# (``diagnostics.in_process_thread_dump``) that works inside every
# HA install variant without privileged capabilities, so py-spy was
# removed to save ~5 MB/image. ``scripts/threaddump-addon.sh`` still
# ``pip install py-spy`` inside its own throwaway sidecar for the
# deadlocked-interpreter edge case, so that path is unaffected.
